---
- name: 'Default | prepare'
  hosts: 'all'
  gather_facts: false
  tasks:
    - name: 'Default | prepare | install packages for verify'
      ansible.builtin.apt:
        name:
          - 'rsync'  # ansible.posix.synchronize
        update_cache: true
        state: 'present'
